#include <stdio.h>
#include <stdlib.h>
/**
*main - returns alphabets bth lower and upercases
*Return: Always  (Success)
*/
int main(void)
{
int ch = 'a';
int CH = 'A';

while (ch <= 'z')
{
putchar(ch);
ch++;
}
c = 'A';
while (CH <= 'Z')
{
putchar(CH);
CH++;
}
putchar('\n')
return (0);
}
