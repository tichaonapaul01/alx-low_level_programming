/**
  * 0-whatsmyname.c - copy memory area
  * @program: prints its name followed by a new line
  * @print the new name without compiling
  * @do not remove the path of the program
  *
  * Return:always (0)
  */

int main(int argc,  char *argv[])
{
          (void)arg 
          printf("%s\n", argv[]);
	  
	  return (0);
}
